version: '3'

services:
  # Redis service for caching and context storage
  redis:
    image: redis:7.2-alpine
    container_name: raven-docs-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --save 900 1 --loglevel warning --maxmemory 2gb --maxmemory-policy allkeys-lru
    networks:
      - raven-docs-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 30s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  # PostgreSQL database service (with pgvector for vector similarity search)
  db:
    image: pgvector/pgvector:pg16
    container_name: raven-docs-postgres
    environment:
      POSTGRES_DB: raven-docs
      POSTGRES_USER: raven-docs_user
      POSTGRES_PASSWORD: raven-docs_secure_password
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - raven-docs-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U raven-docs_user"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  # Memgraph service for agent memory graph relationships
  # Note: Using base image for local dev; production uses memgraph-mage for MAGE algorithms
  memgraph:
    image: memgraph/memgraph:2.11.0
    container_name: raven-docs-memgraph
    ports:
      - "7687:7687"
      - "3005:3000"
    volumes:
      - memgraph_data:/var/lib/memgraph
    networks:
      - raven-docs-network
    restart: unless-stopped

networks:
  raven-docs-network:
    driver: bridge

volumes:
  redis_data:
    driver: local
  db_data:
    driver: local
  memgraph_data:
    driver: local
