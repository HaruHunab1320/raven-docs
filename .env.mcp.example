# MCP Bridge Environment Configuration
# 
# The MCP bridge has been refactored to properly support multi-user systems.
# You only need to provide the server URL and your API key.
# The API key contains all necessary authentication context.

# Server URL (defaults to http://localhost:3000 if not set)
MCP_SERVER_URL=http://localhost:3000

# Your MCP API key (required)
# Generate one using the API key management UI or the register-mcp-api-key.sh script
MCP_API_KEY=your_api_key_here

# Optional: Enable debug logging
# MCP_DEBUG=true
